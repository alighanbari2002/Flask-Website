<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/diseases_style.css') }}">
    <title>Choose Disease</title>
</head>

<body>
    <div class="box">
        <ul>
            {% for category in diseases %}
                <li class="havSub">
                {{category[0]}}
                <div>
                    <ul>
                        {% for disease in category[1] %}
                            <li> <a class="subs" href="{{ url_for('choose_package', user=current_user, disease=disease) }}"> {{disease}} </a> </li>
                        {% endfor %}
                    </ul>
                </div>
            {% endfor %}
        </ul>
    </div>
</body>

</html>
